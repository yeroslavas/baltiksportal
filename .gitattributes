# Normalize line endings: store text as LF in the repo, regardless of OS.
* text=auto eol=lf

# Binary assets — never alter.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
